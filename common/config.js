// 配置信息
export default {
	webUrl:'https://minutemovies.top',// api请求前缀
	// webUrl:'http://play.com',// api请求前缀
	// H5配置
	// #ifdef H5
	//H5接口是浏览器地址，非单独部署不用修改
	//webUrl: window.location.protocol + "//" + window.location.host,
	// #endif
}